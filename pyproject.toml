[project]
name = "fast-image-viewer"
version = "0.1.0"
description = "Step61 MVP for a lightweight image viewer with Alt folder navigation, resizable preview pane, and remembered resize output folder."
requires-python = ">=3.11"
dependencies = [
    "Pillow>=10.0",
]
