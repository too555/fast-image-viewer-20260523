[project]
name = "fast-image-viewer"
version = "0.1.0"
description = "Step12 MVP for a lightweight image viewer with fullscreen preview."
requires-python = ">=3.11"
dependencies = [
    "Pillow>=10.0",
]
